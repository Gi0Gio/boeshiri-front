[build]
  command = "npm run build"
  publish = "dist"

# Las redirecciones viven en public/_redirects, que tiene precedencia sobre este
# archivo. Estaban duplicadas aquí y era fácil tocar una y olvidar la otra.
